@{
    ViewData["Title"] = "Home Page";
}

@* <div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div> *@


@model IEnumerable<Vaga>
@{
    ViewData["Title"] = "Vagas";
}

<div container>
    <nav class="row pt-4">
        <h1 class=col-6>Categorias</h1>
        @* <a asp-controller=Vaga asp-action=Create class="text-rigth col-6 btn btn-primary">Criar</a> *@
    </nav>
    <table class="table" title="Categorias">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Titulo</th>
                <th scope="col">Localizacao</th>
                <th scope="col">Salario</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Vaga vaga in Model)
            {
                <tr>
                    <td width="30%">@vaga.Id</td>
                    <td width="30%">@vaga.Titulo</td>
                    <td width="30%">@vaga.Localizacao</td>
                    <td width="50%">@vaga.Salario</td>
                    @* <td class="text-center">
                        <div class="w-75 btn-group" role="group">
                            <a asp-action="Edit" asp-route-Id="@cat.Id" class="btn btn-warning mx-2">Editar</a>
                            <a asp-action="Delete" asp-route-Id="@cat.Id" class="btn btn-danger">Excluir</a>
                        </div>
                    </td> *@
                </tr>
            }
        </tbody>
    </table>
</div>